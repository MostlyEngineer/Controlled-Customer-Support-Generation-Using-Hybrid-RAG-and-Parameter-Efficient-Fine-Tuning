-r requirements.txt

# Optional packages for the full GPU/Colab capstone workflow.
# Install these in a fresh environment or Colab GPU runtime.
datasets>=2.20
transformers>=4.44
torch>=2.3
accelerate>=0.33
peft>=0.12
bitsandbytes>=0.43; platform_system != "Windows"
sentence-transformers>=3.0
chromadb>=0.5
rouge-score>=0.1.2
nltk>=3.9
evaluate>=0.4
